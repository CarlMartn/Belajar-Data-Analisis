Setup Environment

pip install numpy

pip install pandas

pip install seaborn

pip install streamlit

pip install matplotlib

pip install scipy

pip install -U scikit-learn

pip install plotly

Run Steamlit app

streamlit run Dashboard.py
